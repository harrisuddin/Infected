CREATE TABLE Users(
username VARCHAR(20) PRIMARY KEY,
password VARCHAR(64),
id VARCHAR(64),
)ENGINE=INNODB;

CREATE TABLE Scores(
name VARCHAR(20),
score INT,
FOREIGN KEY(name) REFERENCES Users(username)
)ENGINE=INNODB;


